# M4 音频系统阶段验收

验收日期：2026-08-03  
验收分支：`feature/m4-audio-system`

## 阶段结论

M4 Task 16—21 已形成可审计的两条音频路线，并由统一字幕门禁收口：本地录音经导入、转写、差异检查和分页后生成逐页音频；HeyGen 经本人声音选择、逐页合成与缓存后生成逐页音频。两条路线都会被解析为同一份 `PageAudio[]`，只有完整且一致的单一路线才能进入字幕步骤。

## 已验证能力

- MP3/WAV 原文件保留、16 kHz 单声道规范化、峰值/静音分析、大小与损坏文件阻断。
- faster-whisper `small` 默认 CPU `int8`，支持 CUDA 检测与选择、词级时间戳、模型下载恢复和固定转写文件。
- 中文旁白差异定位与人工处置；未处理普通差异和未处理严重漏读/多读/错读都会阻断。
- 动态规划自动分页、静音偏好、手动边界、撤销和分页 WAV；每页音频缓存键绑定到录音、时间轴和当前旁白 revision。
- HeyGen 私有声音、试听、逐页合成、失败页重试和成功页缓存；已成功页面不会隐式重生或重复计费。
- 旧版 HeyGen 缓存若与当前旁白 revision、声音和语速完全一致，会无付费回填 revision 绑定并记录审计；声音或旁白变化必须显式确认替换，且只重做指定页面。
- 已存在本地录音的项目会在发起请求前阻断单页 HeyGen 替换，避免先计费再因混用门禁失败。
- 签名音频下载不会附带 HeyGen API Key。
- Task 21 统一门禁会阻断：本地/HeyGen 混用、缺页或空音频、路径越界、音频复用、缓存缺失、旧旁白 revision、未完成本地时间轴、未处理差异，以及 HeyGen 声音不一致。
- 后端会阻止直接进入第 6 步；前端在第 5 步显示页面级处理原因，并在通过前禁用第 6、7 步。

## 自动化证据

| 验收项         | 结果         | 覆盖范围                                                           |
| -------------- | ------------ | ------------------------------------------------------------------ |
| Python         | 145/145 通过 | M1—M3 回归、M4 Task 16—21、8 页本地路线、8 页 fake HeyGen 路线     |
| 前端           | 18/18 通过   | 音频导入、差异、分页、门禁提示、设置与既有工作流                   |
| 浏览器生命周期 | 1/1 通过     | 项目创建、关闭浏览器、刷新与恢复                                   |
| 静态检查       | 通过         | Ruff、严格 mypy、ESLint、Prettier、TypeScript                      |
| 生产构建       | 通过         | Vite 与 Remotion                                                   |
| 契约快照       | 9/9 通过     | `project.schema.json` 与 OpenAPI；包含音频 revision 和字幕门禁端点 |

## Task 21 关键回归

- 8 页本地路线真实调用“导入 → 转写 → 差异比较 → 自动分页 → 字幕门禁 → 第 6 步”，得到完整本地 `PageAudio[]`。
- 8 页 fake HeyGen 路线保留第 5 页单独失败并重试；其余成功页始终仅发起一次合成请求，随后完整 `PageAudio[]` 通过字幕门禁。
- 新旁白即使已重新确认，只要页面音频仍绑定旧 revision，就会被标为过期并阻断。
- 网页在转写、差异处置与边界保存后同时刷新项目和音频门禁，避免陈旧结果。

## 保留的 Windows / 真实服务补充验收

以下项目依赖真实模型、Windows 设备或付费账户，自动化 fake 环境不替代其实机证据：

- Windows CPU 环境下载完整 `small` 模型，测量 10 分钟普通话字错率、实时系数、模型大小和峰值内存。
- 人工试听规范化前后的中文 MP3/WAV，确认声道、响度和静音区间符合听感。
- Windows NVIDIA CUDA 环境验证 float16 加速与输出结构一致性。
- Windows DPAPI 保存真实 HeyGen 凭证，并使用本人声音完成两页小额试听和合成。
- 真实 HeyGen 账户验证额度不足、限流以及签名音频地址有效期。
