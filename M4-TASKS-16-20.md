# M4 音频系统 Task 16—20 批次验收

验收日期：2026-08-03  
验收分支：`feature/m4-audio-system`

## 范围结论

Task 16—20 的代码与自动化批次门禁已完成。本记录不包含 Task 21“音频路线统一与阶段门禁”，因此不将当前批次表述为整个 M4 阶段完成。

- Task 16：MP3/WAV 导入、原件保留、FFmpeg 16 kHz mono PCM WAV 规范化、峰值/静音/时长分析和超限阻断。
- Task 17：faster-whisper `small`、默认 CPU `int8`、CUDA 检测与选择、词级时间戳、模型断点下载、暂停恢复和固定转写文件。
- Task 18：中文、数字、缩写与标点归一，遗漏/多读/错读/不确定差异定位、人工处理和审计。
- Task 19：动态规划分页、静音偏好、手动边界、撤销、版本冲突和无重叠/无空洞分页 WAV。
- Task 20：HeyGen 私有声音列表、测试句试听、逐页合成、凭证保护、失败页重试、成功页幂等缓存和请求 ID 审计。

## 自动化证据

| 验收项         | 结果         | 证据                                                          |
| -------------- | ------------ | ------------------------------------------------------------- |
| Python 测试    | 138/138 通过 | M1—M3 回归与 M4 Task 16—20 单元、集成、安全和批次闭环         |
| 前端组件测试   | 16/16 通过   | 导入、设备选择、差异刷新、时间轴刷新、HeyGen 设置和既有工作流 |
| 浏览器生命周期 | 1/1 通过     | Playwright 创建、重开、刷新、暂停/继续                        |
| 静态检查       | 通过         | Ruff、mypy strict、ESLint、Prettier、TypeScript               |
| 生产构建       | 通过         | Vite 与 Remotion 构建                                         |
| 契约漂移       | 通过         | `project.schema.json` 与类型化 OpenAPI 快照                   |

## 本地录音闭环

`tests/integration/test_m4_tasks_16_20_gate.py` 通过真实 API 完成：

1. 导入 16 kHz WAV 并保存规范化记录。
2. 使用确定性 ASR 后端生成带词级时间戳的转写。
3. 将转写与当前确认旁白比较并保存差异。
4. 自动建立 8 页时间轴，导出逐页 WAV。
5. 关闭并重开项目，确认转写、差异、时间轴和页面音频均从 `project.json` 恢复。

分页算法另以 400 词/8 页规模测试限制候选搜索量，避免原始全量平方级候选在约 10 分钟录音上失控；分页结果仍保持单调、连续并优先静音区间。

## HeyGen fake 闭环与安全边界

- 使用当前官方 v3 同步语音接口合同；服务返回 `request_id`、签名音频地址和时长，不沿用旧版任务轮询假设。
- 8 页中第 5 页首次失败后，仅第 5 页发起第二次付费生成请求；其余成功页保持一次请求。
- 缓存键包含旁白 revision、voice ID、语速和语言；成功页面更换参数时返回明确冲突，不隐式重生。
- API Key 只进入受保护的本机配置；设置文件、项目清单、响应和审计均不含明文。
- 下载 HeyGen 返回的签名音频地址时不携带 `x-api-key`，避免凭证发送到 CDN 或其他下载主机。

## 审查补强

- 转写、差异检查和自动分页成功后刷新项目权威状态；差异列表和时间轴会采用重载后的新版本。
- 音频大小限制在落盘前执行，并有小阈值模拟的超限回归。
- CPU 是始终可用的默认设备；仅检测到 CTranslate2 CUDA 设备时向用户提供 CUDA 选项。
- OpenAPI 已包含转写设备查询与设备选择请求体。

## 实机补充验收

以下项目需要外部模型、人工听感、Windows 硬件或真实付费账号，当前自动化结果不替代这些证据：

- Windows CPU-only 环境下载完整 faster-whisper `small` 模型，记录真实 10 分钟普通话字错率、实时系数、模型大小和峰值内存。
- 人工试听 MP3/WAV 规范化前后样本，确认声道、响度和静音分析符合听感。
- 有 NVIDIA CUDA 的 Windows 机器验证设备检测、float16 加速和结果结构一致性。
- Windows DPAPI 保存真实 HeyGen 凭证，并用“本人声音”执行 2 页真实小额试听/合成。
- 真实 HeyGen 账户验证额度不足、限流响应和签名音频地址有效期。

Task 21 将继续负责本地录音与 HeyGen 路线统一、混用禁止、缺页/未确认差异门禁，以及完整 M4 阶段验收。
