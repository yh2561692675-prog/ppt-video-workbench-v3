# P1 生产能力实施基线

**记录日期：** 2026-08-11  
**仓库：** `F:\ppt-video-workbench-v3`  
**分支：** `recovery/root-snapshot-20260810`

## 基线边界

本记录用于保护现有恢复工作，不代表当前工作区已经形成可发布提交。开始七项 P1 能力实施前，先记录 Git 状态并沿用现有文件，不执行 reset、clean、批量覆盖或跨 worktree 复制。

## 当前状态摘要

- 已跟踪改动：120 项
- 未跟踪条目：141 项
- 当前目录存在多个恢复和功能相关 worktree/窗口。
- 统一时间线已有 `ProductionTimeline`、命令引擎、RenderGraph 和前端工作区骨架。
- 本次实施新增内容必须使用独立文件责任和增量契约，不能覆盖现有恢复成果。

## 实施保护规则

1. 共享契约、数据库迁移、主应用 wiring 和主渲染入口串行修改。
2. 新增长任务必须持久化 job_id、输入 fingerprint 和恢复状态。
3. 所有媒体写入使用临时文件、hash 校验和原子替换。
4. 本文件只记录基线；每个项目完成后在对应报告中记录测试与门禁结果。

## 当前已知门禁

- Windows 安装版、Office/LibreOffice、真实 4K/60fps 和批量峰值资源测试需要在对应运行环境中执行。
- 当前根目录改动来自多个既有窗口，正式合并前必须形成可审查提交或恢复停点。
