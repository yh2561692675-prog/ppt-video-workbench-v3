# 冲突登记

| 范围                    | 处理                                                               | 交接动作                                    |
| ----------------------- | ------------------------------------------------------------------ | ------------------------------------------- |
| 公共 API / OpenAPI 入口 | 采用最小修改，新增 AI 路由并同步 `packages/contracts/openapi.json` | 目标分支合并前重跑 contract export/check    |
| Web 公共路由与样式      | 仅增加 `/settings/ai` 和 AI 控制中心样式                           | 集成方复核导航权限与产品视觉冲突            |
| Provider 远端开关       | 默认关闭，只有显式 control-plane switch 才查询远端                 | 不得把默认值改成自动远端                    |
| 本地音频链              | 保持独立，local-only 路由不读取供应商凭证                          | 先验收本地链，再验收外部 provider           |
| 用户既有未提交文档      | 不属于 AI 交接范围，未暂存、未提交                                 | 保留在工作树，集成方不得用 `git add .` 带入 |

没有修改 `program-core-workbench`，没有触碰 DP45 运行进程、运行根或证据，没有执行最终候选替换或分支合并。
