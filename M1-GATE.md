# M1 基础工程验收记录

日期：2026-08-03  
分支：`feature/m1-foundation`

## 自动化验收结果

- [x] API、Web、Remotion 三个工作区可发现并由统一质量命令管理。
- [x] Python 领域契约、JSON Schema 与 OpenAPI 快照测试通过。
- [x] `project.json` 使用同目录临时文件、`fsync`、`os.replace` 与 `.bak` 实现原子保存和恢复。
- [x] 原子保存完成 100 次故障注入循环，模拟中断后旧清单均保持有效。
- [x] SQLite 持久任务在 30% 和 70% 进度中断后恢复为 `paused`。
- [x] 完成任务按项目＋缓存键幂等复用；普通任务指数退避，付费任务最多尝试 2 次。
- [x] 本地网页工作台可创建中文项目、显示磁盘空间、使用七步导航、暂停和继续。
- [x] Playwright 实测关闭页面后从项目中心重开，步骤和状态仍由后端恢复。
- [x] 创建 3 个中文项目并连续模拟 10 次应用关闭/重启，清单均无损坏。
- [x] 删除 `workspace.db` 后，从三个项目的 `project.json` 成功重建索引。

## 环境边界

当前自动化运行环境为 Linux，已验证跨平台核心逻辑和 Chromium 浏览器工作流。以下项目必须在 M7/M8 的 Windows 10/11 64 位实机中再次执行后才能签署 Windows 验收：

- [ ] 在包含中文用户名和中文磁盘路径的 Windows 环境运行 `powershell -File scripts/check.ps1`。
- [ ] 使用任务管理器强制结束桌面工作台进程 10 次并恢复三个真实项目。
- [ ] 核对 Windows 文件锁、DPAPI、安装器、桌面快捷方式和升级回滚行为。

M1 Task 1—5 的代码与自动化门禁可进入下一阶段；Windows 专属门禁仍保持待验收状态。
